version: "3.8"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    environment:
      - GOOGLE_API_KEY=${GOOGLE_API_KEY}
      - DATABASE_URL=sqlite:///data/harness_foundry.db
    volumes:
      - ./data:/app/data
